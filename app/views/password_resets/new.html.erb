<div class="h-screen w-screen flex justify-center items-center">
  <div>
    <div class="text-center">
      <h2 class="text-3xl mb-3 font-bold">パスワードリセット</h2>
    </div>
    <div class="text-center">
      <p class="text-1xl mb-3">登録済みのメールアドレスにパスワード再設定用URLを送信します。</h2>
    </div>
    <div>
      <%= form_with url: password_resets_path, local: true do |f| %>
        <div>
          <%= f.label :email, User.human_attribute_name(:email) %>
          <%= f.email_field :email, class: 'w-full p-2 border border-indigo-600' %>
        </div>
        <div>
          <%= f.submit t('defaults.register'), class: 'mt-3 btn btn-primary py-1 px-8' %>
        </div>
      <% end %>
    </div>
  </div>
<div>

